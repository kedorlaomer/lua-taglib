# Lua bindings for ID3v2

This is a rudimentary attempt at using [taglib](http://taglib.org/) from lua.

# Documentation

Everything you can do with this module is shown in [example.lua](example.lua).
